document.addEventListener("DOMContentLoaded", () => {
    const newCommentSection = document.getElementById('newComment');

if(newCommentSection) {
newCommentSection.addEventListener("submit", async (event) => {
event.preventDefault();

const commentData = new commentData(newCommentSection);
const 

})

}

})