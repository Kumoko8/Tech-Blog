<h1> The Tech Blog</h1>
{{#each posts as |post|}}
<section>
    <section>
<h2>{{post.title}}</h2>
    </section>
        <p>Posted by: {{post.author}} on {{post.created_on}}</p>
    <section>
        <p> {{post.description}}</p>
    </section>
</section>
{{/each}}